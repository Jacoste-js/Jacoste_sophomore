jihyukim
```js
function solution(book_time) {
    const calc = (time) => {
        const [h, m] = time.split(':');
        return parseInt(h) * 60 + parseInt(m);
    };
    const list = book_time.map(([from, to]) => [calc(from), calc(to)]).sort((a, b) => a[0] - b[0]);
    const rooms = [];
    
    list.forEach(([from, to]) => {
        let min = to;
        let idx = -1;
        rooms.forEach((room, i) => {
            if (from < room + 10) return;
            if (room - from < min) {
                idx = i;
                min = room - from;
            }
        })
        if (idx === -1 || rooms.length === 0) rooms.push(to);
        else rooms[idx] = to;
    })
    return rooms.length;
}
```
jabae
```js
// code
```
daekim
```js
// code
```
jiyokim
```js
//code
```
rjeong
```js
//code
```
